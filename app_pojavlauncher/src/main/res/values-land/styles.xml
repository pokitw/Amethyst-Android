<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Landscape changes exactly one thing about the app theme: text is a step larger. Everything
        else — including the dialog styling — comes from AmethystXBaseTheme in values/styles.xml,
        so the two orientations cannot drift apart again.
    -->
    <style name="AppTheme" parent="AmethystXBaseTheme">
        <item name="android:textSize">@dimen/_14ssp</item>
    </style>

    <style name="LauncherMenuButton.Universal" parent="LauncherMenuButton">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

</resources>
